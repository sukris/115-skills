# 115 Skills 重构开发任务列表

> **方案：** C（混合模式）  
> **开始时间：** 2026-03-15 17:40 GMT+8  
> **要求：** 每个任务节点必须测试

---

## 📋 任务总览

| 阶段 | 任务数 | 预计时间 | 状态 |
|------|--------|----------|------|
| 阶段 1：核心优化 | 4 | 2 小时 | ⏳ 待开始 |
| 阶段 2：新功能 | 4 | 4 小时 | ⏳ 待开始 |
| 阶段 3：体验优化 | 3 | 3 小时 | ⏳ 待开始 |
| **总计** | **11** | **9 小时** | ⏳ |

---

## 📝 阶段 1：核心优化（P0 - 立即实施）

### 任务 1.1：上下文管理器

**目标：** 实现会话上下文管理（路径、选中文件、历史操作）

**实现内容：**
- [ ] 创建 `lib/context/session-context.js`
- [ ] 实现路径管理（当前目录、面包屑导航）
- [ ] 实现选中文件管理
- [ ] 实现操作历史记录
- [ ] 实现上下文序列化/反序列化

**测试用例：**
```javascript
// test/context/session-context.test.js
describe('SessionContext', () => {
  test('设置和获取当前路径', () => {})
  test('面包屑导航生成', () => {})
  test('选中文件管理', () => {})
  test('操作历史记录', () => {})
  test('上下文序列化', () => {})
})
```

**验收标准：**
- ✅ 路径切换正确
- ✅ 面包屑导航正确
- ✅ 选中文件可添加/清除
- ✅ 历史记录可追溯

**优先级：** 🔴 P0  
**预计时间：** 30 分钟  
**状态：** ⏳ 待开始

---

### 任务 1.2：快捷命令解析器

**目标：** 支持快捷命令和数字选择

**实现内容：**
- [ ] 创建 `lib/parser/command-parser.js`
- [ ] 支持 `/115 [命令] [参数]` 格式
- [ ] 支持数字选择（1-9）
- [ ] 支持自然语言命令（打开、返回等）
- [ ] 支持快捷词（容量、清理、分享等）

**测试用例：**
```javascript
// test/parser/command-parser.test.js
describe('CommandParser', () => {
  test('/115 搜索 工作报告', () => {})
  test('数字选择 1', () => {})
  test('自然语言 打开工作文档', () => {})
  test('快捷词 容量', () => {})
  test('无效命令处理', () => {})
})
```

**验收标准：**
- ✅ 命令解析准确率 > 95%
- ✅ 数字选择响应 < 100ms
- ✅ 支持 10+ 快捷词

**优先级：** 🔴 P0  
**预计时间：** 30 分钟  
**状态：** ⏳ 待开始

---

### 任务 1.3：主动推荐系统

**目标：** 根据操作场景主动推荐下一步

**实现内容：**
- [ ] 创建 `lib/recommender/action-recommender.js`
- [ ] 容量查询后推荐清理
- [ ] 文件浏览后推荐操作
- [ ] 搜索后推荐批量操作
- [ ] 下载完成后推荐整理

**测试用例：**
```javascript
// test/recommender/action-recommender.test.js
describe('ActionRecommender', () => {
  test('容量查询推荐', () => {})
  test('文件浏览推荐', () => {})
  test('搜索后推荐', () => {})
  test('下载完成推荐', () => {})
})
```

**验收标准：**
- ✅ 每个场景有 2-3 个推荐
- ✅ 推荐相关性 > 80%
- ✅ 推荐响应 < 50ms

**优先级：** 🔴 P0  
**预计时间：** 30 分钟  
**状态：** ⏳ 待开始

---

### 任务 1.4：错误处理优化

**目标：** 友好错误提示 + 恢复建议

**实现内容：**
- [ ] 创建 `lib/error/error-handler.js`
- [ ] 错误分类（网络、权限、参数等）
- [ ] 友好错误消息
- [ ] 恢复建议
- [ ] 错误日志记录

**测试用例：**
```javascript
// test/error/error-handler.test.js
describe('ErrorHandler', () => {
  test('网络错误处理', () => {})
  test('权限错误处理', () => {})
  test('参数错误处理', () => {})
  test('恢复建议生成', () => {})
})
```

**验收标准：**
- ✅ 所有错误有友好提示
- ✅ 80% 错误有恢复建议
- ✅ 错误日志完整

**优先级：** 🔴 P0  
**预计时间：** 30 分钟  
**状态：** ⏳ 待开始

---

## 📝 阶段 2：新功能（P1 - 本周实施）

### 任务 2.1：分享管理功能

**目标：** 实现分享创建、查询、更新、取消

**实现内容：**
- [ ] 更新 `lib/share/transfer.js`
- [ ] 创建分享（`/share/send`）
- [ ] 获取分享信息（`/share/shareinfo`）
- [ ] 更新分享时长（`/share/updateshare`）
- [ ] 获取访问列表（`/share/access_user_list`）
- [ ] 取消分享

**测试用例：**
```javascript
// test/share/share-management.test.js
describe('ShareManagement', () => {
  test('创建分享', () => {})
  test('获取分享信息', () => {})
  test('更新分享时长', () => {})
  test('获取访问列表', () => {})
  test('取消分享', () => {})
})
```

**验收标准：**
- ✅ 5 个 API 全部测试通过
- ✅ 分享创建 < 2 秒
- ✅ 分享信息准确

**优先级：** 🟡 P1  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

### 任务 2.2：批量操作功能

**目标：** 支持批量移动、复制、删除

**实现内容：**
- [ ] 更新 `lib/files/operations.js`
- [ ] 批量选择文件
- [ ] 批量移动（`/files/move`）
- [ ] 批量复制（`/files/copy`）
- [ ] 批量删除（`/rb/delete`）
- [ ] 进度显示

**测试用例：**
```javascript
// test/files/batch-operations.test.js
describe('BatchOperations', () => {
  test('批量选择文件', () => {})
  test('批量移动', () => {})
  test('批量复制', () => {})
  test('批量删除', () => {})
  test('进度显示', () => {})
})
```

**验收标准：**
- ✅ 支持 100+ 文件批量操作
- ✅ 批量操作有进度显示
- ✅ 错误处理完善

**优先级：** 🟡 P1  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

### 任务 2.3：离线下载管理

**目标：** 实现下载任务管理

**实现内容：**
- [ ] 更新 `lib/lixian/download.js`
- [ ] 添加磁力任务
- [ ] 添加 HTTP 任务
- [ ] 获取任务列表
- [ ] 删除任务
- [ ] 获取下载进度

**测试用例：**
```javascript
// test/lixian/download-management.test.js
describe('DownloadManagement', () => {
  test('添加磁力任务', () => {})
  test('添加 HTTP 任务', () => {})
  test('获取任务列表', () => {})
  test('删除任务', () => {})
  test('下载进度', () => {})
})
```

**验收标准：**
- ✅ 任务添加成功
- ✅ 任务列表准确
- ✅ 进度更新及时

**优先级：** 🟡 P1  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

### 任务 2.4：智能清理功能

**目标：** 分析文件并给出清理建议

**实现内容：**
- [ ] 创建 `lib/organizer/clean-advisor.js`
- [ ] 回收站文件统计
- [ ] 重复文件检测
- [ ] 大文件检测
- [ ] 旧文件检测
- [ ] 清理建议生成

**测试用例：**
```javascript
// test/organizer/clean-advisor.test.js
describe('CleanAdvisor', () => {
  test('回收站统计', () => {})
  test('重复文件检测', () => {})
  test('大文件检测', () => {})
  test('旧文件检测', () => {})
  test('清理建议生成', () => {})
})
```

**验收标准：**
- ✅ 检测准确率 > 90%
- ✅ 建议可操作性强
- ✅ 清理操作安全

**优先级：** 🟡 P1  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

## 📝 阶段 3：体验优化（P2 - 2 周内）

### 任务 3.1：卡片式响应

**目标：** 使用 Markdown 表格/代码块美化响应

**实现内容：**
- [ ] 创建 `lib/ui/response-builder.js`
- [ ] 文件列表卡片
- [ ] 容量信息卡片
- [ ] 任务进度卡片
- [ ] 确认对话框

**测试用例：**
```javascript
// test/ui/response-builder.test.js
describe('ResponseBuilder', () => {
  test('文件列表卡片', () => {})
  test('容量信息卡片', () => {})
  test('任务进度卡片', () => {})
  test('确认对话框', () => {})
})
```

**验收标准：**
- ✅ 卡片美观易读
- ✅ 响应格式一致
- ✅ 移动端友好

**优先级：** 🟢 P2  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

### 任务 3.2：进度显示

**目标：** 实时显示操作进度

**实现内容：**
- [ ] 更新 `lib/ui/response-builder.js`
- [ ] 下载进度条
- [ ] 上传进度条
- [ ] 整理进度条
- [ ] 批量操作进度

**测试用例：**
```javascript
// test/ui/progress-display.test.js
describe('ProgressDisplay', () => {
  test('下载进度条', () => {})
  test('上传进度条', () => {})
  test('整理进度条', () => {})
})
```

**验收标准：**
- ✅ 进度实时更新
- ✅ 进度条美观
- ✅ 预计时间准确

**优先级：** 🟢 P2  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

### 任务 3.3：历史记录

**目标：** 记录最近操作，支持快速返回

**实现内容：**
- [ ] 更新 `lib/context/session-context.js`
- [ ] 操作历史记录
- [ ] 最近文件记录
- [ ] 快速返回功能
- [ ] 历史搜索

**测试用例：**
```javascript
// test/context/history.test.js
describe('History', () => {
  test('操作历史记录', () => {})
  test('最近文件记录', () => {})
  test('快速返回', () => {})
  test('历史搜索', () => {})
})
```

**验收标准：**
- ✅ 记录最近 20 个操作
- ✅ 快速返回 < 1 秒
- ✅ 历史搜索准确

**优先级：** 🟢 P2  
**预计时间：** 60 分钟  
**状态：** ⏳ 待开始

---

## 📊 测试要求

### 每个任务必须包含

1. **单元测试** - 测试函数逻辑
2. **集成测试** - 测试 API 调用
3. **手动测试** - 实际场景验证

### 测试报告格式

```markdown
### 任务 X.X：[任务名称]

**测试时间：** YYYY-MM-DD HH:mm

**单元测试：**
- [ ] 用例 1 ✅
- [ ] 用例 2 ✅

**集成测试：**
- [ ] API 1 ✅
- [ ] API 2 ✅

**手动测试：**
- [ ] 场景 1 ✅
- [ ] 场景 2 ✅

**测试结果：** ✅ 通过 / ❌ 失败

**问题记录：**
- 问题 1（如有）
- 问题 2（如有）
```

---

## 🎯 执行顺序

```
阶段 1（核心优化）
├─ 任务 1.1：上下文管理器
├─ 任务 1.2：快捷命令解析器
├─ 任务 1.3：主动推荐系统
└─ 任务 1.4：错误处理优化

阶段 2（新功能）
├─ 任务 2.1：分享管理功能
├─ 任务 2.2：批量操作功能
├─ 任务 2.3：离线下载管理
└─ 任务 2.4：智能清理功能

阶段 3（体验优化）
├─ 任务 3.1：卡片式响应
├─ 任务 3.2：进度显示
└─ 任务 3.3：历史记录
```

---

## 📈 进度追踪

| 阶段 | 总任务 | 完成 | 进行中 | 待开始 | 完成率 |
|------|--------|------|--------|--------|--------|
| 阶段 1 | 4 | 0 | 0 | 4 | 0% |
| 阶段 2 | 4 | 0 | 0 | 4 | 0% |
| 阶段 3 | 3 | 0 | 0 | 3 | 0% |
| **总计** | **11** | **0** | **0** | **11** | **0%** |

---

**文档位置：** `docs/重构开发任务列表.md`  
**开始时间：** 2026-03-15 17:40 GMT+8  
**预计完成：** 2026-03-15 26:40 GMT+8（约 9 小时）

---

**小主，任务列表已创建！现在开始执行任务 1.1：上下文管理器？** 🦐
